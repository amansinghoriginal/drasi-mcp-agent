# MCP Events Design Sketch — findings from the serverless-agent (webhook-consumer) effort

Observations on gaps/ambiguities in the draft MCP Events extension discovered while building an
event-driven, scale-to-zero Dapr agent that consumes the **webhook** delivery mode. This is the
*consumer / subscriber* vantage point — complementary to the server-side findings in
`../drasi-mcp-events/SPEC-GAPS.md`.

> Status: collected during build + integration; synthesis pending.
